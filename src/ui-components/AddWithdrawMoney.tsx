'use client'
import { useState } from 'react';

export default function AddWithdrawMoney({title, total, target, percent}: {title: string; total: number; target: number; percent: number}) {
    return (
        <form>
            <p></p>
        </form>
    );
}